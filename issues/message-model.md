# Document Title

each conversation could be a separate csvs dataset in a zoo. each messenger could be a separate zoo.

each conversation could be a separate fountain file. each messenger could be a separate id provider. so conversations for bob in whatsapp in one file, for bob in telegram in another file.
